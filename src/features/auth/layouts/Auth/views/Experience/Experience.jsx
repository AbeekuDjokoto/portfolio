import classes from "./Experience.module.scss";

export const Experience = () => {
  return (
    <div className={classes.root}>
      <div className={classes.rightPanel}>This is the right</div>
    </div>
  );
};
